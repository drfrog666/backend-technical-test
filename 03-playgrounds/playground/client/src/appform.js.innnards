     <form action="/locations/create" method="post" onClick={() => this.saveMe(location)}>
            Create Location: <input name="location" onEnter={() => this.saveMe(location)}></input>
            <button type="submit">submit</button>
        </form>
